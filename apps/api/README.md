# apps/api

FastAPI backend application.

Handles HTTP endpoints, request validation, streaming, and dependency injection.
Must not contain analytical or business logic — delegates to packages.

## Run locally

```bash
make api
```
