# packages/orchestration

Run engine and workflow state machine.

Decides reuse vs hybrid vs fresh, invokes agents in order, emits run events,
handles failure states, routes clarification pauses and resumes.

Implementation deferred to Phase 4.
